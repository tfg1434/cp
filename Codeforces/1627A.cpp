#include <bits/stdc++.h>
using namespace std;

#define ll long long

int T, R, C, N, M;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> T;
    while (T--) {
        
    }

    return 0;
}
