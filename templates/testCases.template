#include <bits/stdc++.h>
using namespace std;

int T;


#define ll long long

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    cin >> T;
    while (T--) {
        
    }    
    
    return 0;
}
